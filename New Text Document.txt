CREATE DATABASE company_db2;
USE company_db2;
CREATE TABLE Employees (
    FirstName VARCHAR(50),
    LastName VARemployeesCHAR(50),
    Department VARCHAR(100)
);
CREATE TABLE faculty (
    EmployeeID INT PRIMARY KEY AUTO_INCREMENT,
    FirstName VARCHAR(50) NOT NULL,
    LastName VARCHAR(50) NOT NULL,
    Department VARCHAR(100),
    Role VARCHAR(100)
);
INSERT INTO faculty VALUES (1001,'m hammad','shakeel','AI','Engineer');
SELECT * FROM faculty;
INSERT INTO Employees (FirstName, LastName, Department)
SELECT FirstName, LastName, Department FROM faculty;

SELECT * FROM Employees;
SHOW COLUMNS FROM Employees;
TRUNCATE TABLE Employees;
INSERT INTO Employees (FirstName, LastName, Department,Role)
SELECT FirstName, LastName, Department,Role FROM faculty;
SELECT * FROM faculty;
SHOW COLUMNS FROM Employees;

ALTER TABLE Employees ADD COLUMN Role VARCHAR(50);

SHOW COLUMNS FROM Employees;
INSERT INTO Employees (Role)
SELECT Role FROM faculty;
